apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent-service
  template:
    metadata:
      labels:
        app: agent-service
    spec:
      containers:
      - name: agent-service
        image: cdquang/my-app:agent-service-latest
        imagePullPolicy: Always 
        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"
        ports:
        - containerPort: 5000
        env:
        - name: CHROMA_HOST
          value: "chromadb-service"
        - name: CHROMA_PORT
          value: "8000"
        - name: EUREKA_SERVER_URL
          value: "http://discovery-server:8761"
        - name: EUREKA_APP_NAME
          value: "AGENT-SERVICE"
        - name: CHROME_MCP_URL
          value: "http://chrome-mcp:8000"
        - name: CHROME_MCP_TOOL
          value: "browser.navigate"
---
apiVersion: v1
kind: Service
metadata:
  name: agent-service
spec:
  selector:
    app: agent-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP